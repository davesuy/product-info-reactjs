	// var id = response.map( (x) => {

			// 	return <div className="api-id" key={x.id} >{x.id}</div>

			// });

			// var title = response.map( (x) => {

		

			// 	return <h2 className="api-title" key={x.title.rendered} dangerouslySetInnerHTML={{ __html: x.title.rendered }} />


			// });

			// var content = response.map( (x) => {

				

			// 	return <div className="api-content" key={x.id} dangerouslySetInnerHTML={{ __html: x.content.rendered }} />

			// });

			

			// this.setState({

			// 	id: id,
			// 	title: title,
			// 	content: content

			// });
			
